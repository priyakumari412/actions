name: Run Node.js Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - name: ⬇️ Checkout code
      uses: actions/checkout@v3

    - name: 🟢 Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: 18  # You can change to 16 or whatever you're using

    - name: 📦 Install dependencies
      run: npm install

    - name: 🧪 Run tests
      run: npm test
